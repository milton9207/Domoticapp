<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">
    <item android:id="@+id/action_settings"
          android:title="@string/action_settings"
          android:orderInCategory="100"
          app:showAsAction="never"/>

    <item android:id="@+id/action_settings_layer1"
          android:title="Layer1"
          app:showAsAction="never"
            />
    <item android:id="@+id/action_settings_layer2"
          android:title="Layer2"
          app:showAsAction="never"
            />

    <item android:id="@+id/action_select_layer"
          android:title="@string/select_layer"
          android:icon="@drawable/ic_action"
          app:showAsAction="ifRoom|withText"/>
</menu>
